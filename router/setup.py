from setuptools import setup, find_packages

setup(
    name="router",
    version="0.3",
    packages=find_packages(),
    description="Simulador de roteador",
    author="Robson Santos",
    install_requires=[],
    python_requires='>=3.6',
)